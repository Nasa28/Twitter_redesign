<% @user.followed.includes(:follower).each do |user| %>
      <div class = 'd-flex justify-content-around mt-2 align-items-center'>
        <%= profile_photo(user.follower) %>
        <strong class ="pl-2"><%= link_to  user.follower.full_name.split.map(&:capitalize)*' ',
         user_path(user.follower), class: "text-decoration-none"%></strong>
      <div>  
    <% end %>